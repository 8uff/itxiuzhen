今天完成的事情：
任务6主内容导航部分实现。
学习bootstrap

明天计划的事情：
继续任务6实现，继续学习bootstrap。

遇到的问题：
 boostrap导航折叠断点
 通过媒体查询修改分隔小竖线出现宽度。

 给span设置margin padding 失效。
 原因：行内元素的内边距对左、右、下起作用。行内元素的外边距只对左、右起作用。


 通过媒体查询设置当；浏览器宽度大于1200px时，页面内容不在自适应页浏览器宽度，固定为1200px。但是fixed元素宽度仍自适应，因为当position是fixed或absolute时，百分比是参考屏幕的宽度。
 解决方法：通过媒体查询手动设置fixed元素宽度。
下表总结了 Bootstrap 网格系统如何跨多个设备工作：

超小设备手机（<768px）	小型设备平板电脑（≥768px）	中型设备台式电脑（≥992px）	大型设备台式电脑（≥1200px）
网格行为	一直是水平的	以折叠开始，断点以上是水平的	以折叠开始，断点以上是水平的	以折叠开始，断点以上是水平的
最大容器宽度	None (auto)	750px	970px	1170px
Class 前缀	.col-xs-	.col-sm-	.col-md-	.col-lg-
列数量和	12	12	12	12
最大列宽	Auto	60px	78px	95px
间隙宽度	30px
（一个列的每边分别 15px）	30px
（一个列的每边分别 15px）	30px
（一个列的每边分别 15px）	30px
（一个列的每边分别 15px）
可嵌套	Yes	Yes	Yes	Yes
偏移量	Yes	Yes	Yes	Yes
列排序	Yes	Yes	Yes	Yes

text-overflow: clip|ellipsis|string;
text-overflow 属性规定当文本溢出包含元素时发生的事情。

white-space:nowrap;如何禁止在元素中的文本折行
normal	默认。空白会被浏览器忽略。
pre	空白会被浏览器保留。其行为方式类似 HTML 中的 <pre> 标签。
nowrap	文本不会换行，文本会在在同一行上继续，直到遇到 <br> 标签为止。
pre-wrap	保留空白符序列，但是正常地进行换行。
pre-line	合并空白符序列，但是保留换行符。

overflow: hidden;
这个属性定义溢出元素内容区的内容会如何处理。
visible	默认值。内容不会被修剪，会呈现在元素框之外。
hidden	内容会被修剪，并且其余内容是不可见的。
scroll	内容会被修剪，但是浏览器会显示滚动条以便查看其余的内容。
auto	如果内容被修剪，则浏览器会显示滚动条以便查看其余的内容。